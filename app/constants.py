"""
Shared constants across the application
"""

# Minimum similarity threshold for fuzzy product matching (0.0 to 1.0)
# Used for EPB products to match with existing database products
MIN_FUZZY_MATCH_THRESHOLD = 0.6  # 60% similarity required for a match
